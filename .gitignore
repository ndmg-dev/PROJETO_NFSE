# Segredos — nunca versionar (spec §8)
*.pfx
*.p12
*.pem
*.key
.env
.env.*

# Saída da PoC
poc/out/

# Python
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
poc/out/

# o exemplo é público de propósito — não contém segredo
!.env.exemplo

# PDFs soltos na raiz são notas reais de clientes (conta bancária, e-mail,
# CNPJ). O repositório é público: nunca versionar. Para teste, usar XML
# anonimizado (spec §10).
/*.pdf

# jsdom das verificações de tests/web
node_modules/

# registro da instalação (iniciar.sh / Iniciar.bat)
instalacao.log

# gerado por make test-windows
.tmp-windows
dist/
